# Go-Trust Configuration Example
#
# This file configures the go-trust AuthZEN PDP server.
# Use with: gt --config config.yaml

# Server settings
server:
  # Host to listen on (default: 127.0.0.1)
  host: "0.0.0.0"

  # Port to listen on (default: 6001)
  port: "6001"

  # External URL for PDP discovery (optional)
  # Set this when running behind a reverse proxy
  external_url: "https://pdp.example.com"

# Logging configuration
logging:
  # Log level: debug, info, warn, error (default: info)
  level: "info"

  # Log format: text or json (default: text)
  # Use json for production/structured logging
  format: "json"

# ETSI TSL Registry configuration
etsi:
  # PEM file containing trusted CA certificates
  # Generated by tsl-tool from g119612
  cert_bundle: "/etc/go-trust/trusted-certs.pem"

  # Alternatively, load raw TSL XML files directly
  # tsl_files:
  #   - "/var/lib/tsls/eu-lotl.xml"
  #   - "/var/lib/tsls/se-tsl.xml"

# Rate limiting (requests per second per IP)
# Set to 0 to disable
rate_limit_rps: 100
